.PHONY: setup test lint format tree

setup:
	@echo "Bootstrap phase: install commands are not wired yet."

test:
	@echo "Bootstrap phase: no business-logic tests are implemented yet."

lint:
	@echo "Bootstrap phase: lint commands are not wired yet."

format:
	@echo "Bootstrap phase: format commands are not wired yet."

tree:
	@tree -L 3
